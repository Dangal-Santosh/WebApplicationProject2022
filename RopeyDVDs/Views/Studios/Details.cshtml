@model RopeysDVD.Models.Studio

@{
    ViewData["Title"] = "Details";
}

<h4 class="text-danger fw-bold text-center">Studio Details</h4>

<div class="w-50 m-auto bg-secondary" style="padding: 20px 15px; border-radius: 5px">
    <hr />
    <dl class="row bg-secondary">
        <dt class = "col-sm-2 text-center text-white ml-4">
            @Html.DisplayNameFor(model => model.StudioName)
        </dt>
        <dd class = "col-sm-10 text-center text-white ml-4">
            @Html.DisplayFor(model => model.StudioName)
        </dd>
    </dl>
<div>
    <a asp-action="Edit" class="btn btn-success m-lg-1" asp-route-id="@Model?.StudioNumber">Edit</a> 
    <a asp-action="Index" class="btn btn-info m-lg-1">Back to List</a>
</div>
</div>